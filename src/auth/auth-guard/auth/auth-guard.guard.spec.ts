import { Auth\authGuardGuard } from './auth\auth-guard.guard';

describe('Auth\authGuardGuard', () => {
  it('should be defined', () => {
    expect(new Auth\authGuardGuard()).toBeDefined();
  });
});
